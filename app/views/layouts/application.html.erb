<!DOCTYPE html>
<html>
<head>
  <title>Brewpedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <ul class="nav navbar-nav">
      <li id="logo"><%= link_to root_path do %>
          
           <% end %>
       </li>
       <li id="tab"><%= link_to "Public Wikis", wikis_path %></li>

      <% if user_signed_in? %>
         <li id="tab"><%= link_to "My Wikis", user_path(current_user) %></li>

          <% if current_user.standard? %>
            <li id="tab"><%= button_to "Upgrade", charges_path, class: 'btn btn-success' %></li>
          <% end %>
       <% end %>
     </ul>
     <div class="pull-right user-info">
      <% if user_signed_in? %>
        <strong><%= link_to current_user.name, edit_user_registration_path, :class => 'navbar-link' %></strong> |
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link' %>
      <% else %>
        <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link' %>
        <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link' %>
      <% end %>
    </div>


    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </div>

</body>
</html>
